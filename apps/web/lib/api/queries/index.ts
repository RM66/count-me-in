/**
 * Client-side query hooks (React Query).
 * GET requests organized by domain.
 *
 * TODO: Add query hooks as API endpoints are implemented:
 * - useOrganizer(slug)
 * - useServices(organizerId)
 * - useService(serviceId)
 * - useSlots(serviceId)
 * - useBookings(organizerId)
 */

export * from './organizer'
