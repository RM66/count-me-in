[project]
name = "countmein-migration-tools"
version = "0.0.0"
description = "Phase 1 parity recorder tooling (record.py, sink.py) — not part of the deployed API"
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.27",
  "pyyaml>=6",
  "redis>=5",
  "psycopg[binary]>=3.2",
]

[tool.uv]
package = false
