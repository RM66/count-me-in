{
  "name": "count-me-in",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "generate:openapi": "turbo run generate:openapi",
    "generate:constants": "turbo run generate:constants",
    "generate:i18n": "turbo run generate:i18n",
    "knip": "knip --exclude duplicates"
  },
  "devDependencies": {
    "knip": "^6.37.0",
    "prettier": "^3.9.8",
    "turbo": "^2.11.2",
    "typescript": "7.0.2"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "node": ">=22"
  },
  "devEngines": {
    "packageManager": {
      "name": "bun",
      "version": "1.3.14"
    }
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
