# Go → Python API Migration Plan (autonomous-agent runbook)

Scope: replace the Go API in `apps/web/api` + `apps/web/pkg` (module `countmein`, single Vercel Function `api/entry/index.go`) with a Python/FastAPI API with **byte-compatible HTTP behavior**. Next.js (`apps/web/src`), Auth.js (`src/app/api/auth/[...nextauth]`), `packages/*` (Drizzle schema + migrations stay the schema owner), and the Zod contract manifests (`packages/contracts/src/wire.ts`, `routes.ts`) are **not** migrated and must not change behavior.

## 0. Global rules (apply to every step)

1. **Source of truth for the wire:** `apps/web/openapi.yaml`, generated from Zod by `bun run generate:openapi`. Python never hand-edits it. Python models are generated _from_ it.
2. **Source of truth for behavior:** the Go code and its tests _at the commit where Phase 1 starts_ (record it as `GO_BASELINE_SHA` in `docs/migration/baseline.txt`). When Go code and docs disagree, Go code wins; log the discrepancy in `docs/migration/discrepancies.md`. **Resolution rule:** a discrepancy is _ported as-is_ to Python (byte-compatible behavior is the goal), recorded in `discrepancies.md`, and only fixed — deliberately, in both sides or in the spec — after the Phase 6 cutover. Never "improve" behavior mid-migration.
3. **Never edit `*_gen.py`** (same rule as `*_gen.go`). Hand-written code lives in non-`_gen` files.
4. Do not stage or commit unless the orchestrating user/pipeline instructs it. Each phase ends with a "gate" — all its verification commands must exit 0 before the next phase starts. If a gate fails 3 times in a row on the same cause, stop and write the failure to `docs/migration/blockers.md`.
5. Working directory for all commands is `apps/web/` unless stated otherwise.
6. Go stays buildable and deployed until Phase 6 cutover gate passes locally; Go source is deleted in Phase 5 only after the parity gate (5.0) is green.
7. Preserve every invariant in `AGENTS.md` → "Conventions" (atomic seat reserve, demo organizer write refusal, consumed guest ticket, QStash after commit, outbox idempotency, manage-token hashing/expiry, delete 409 guards, media cleanup, healthz, rate limiting, `TRUST_PROXY_HEADERS`, `STRICT_ENV`, i18n of API errors). Each one gets an explicitly named test (see 4.3).

## Fixed technology choices

| Concern                                  | Choice (pinned major)                                                                                                   | Why it fits Vercel serverless                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Python                                   | **3.12** (`.python-version` = `3.12`)                                                                                   | Supported by Vercel Python runtime; stable wheels for all deps below.                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Dependency mgmt                          | **uv** (`pyproject.toml` + `uv.lock`), exported to `apps/web/requirements.txt` for Vercel                               | Deterministic lock; Vercel's runtime reliably installs `requirements.txt`. CI fails if export is stale.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Web framework                            | **FastAPI 0.115+**, ASGI app exported as `app`                                                                          | Vercel Python runtime serves ASGI apps natively; one app = one "fat" function (mirrors current Go design, one cold start shared by all routes).                                                                                                                                                                                                                                                                                                                                                                             |
| Schemas                                  | **Pydantic v2** models generated by **datamodel-code-generator** from `openapi.yaml`                                    | Rust core → fast validation; codegen keeps spec↔code drift a CI failure (replaces oapi-codegen).                                                                                                                                                                                                                                                                                                                                                                                                                            |
| DB access                                | **SQLAlchemy 2.0 Core (async)** + **psycopg 3** (`psycopg[binary]`), `NullPool`, `prepare_threshold=None`               | No ORM identity-map overhead; Core expresses the conditional `UPDATE … WHERE booked_count + :seats <= capacity` exactly; `NullPool` + no server-side prepared statements is safe behind a transaction-mode pooler (PgBouncer/Neon/Supabase) and does not leak connections across frozen serverless instances. asyncpg rejected: needs statement-cache workarounds behind PgBouncer. Prisma Client Python rejected: extra Node/Rust engine binary bloats bundle and cold start, and would duplicate Drizzle as schema owner. |
| Redis                                    | **redis-py 5 (`redis.asyncio`)**                                                                                        | Same wire semantics as go-redis (`SET NX EX`, `GETDEL`). Client created per invocation-scope lazily, no pool warm-up.                                                                                                                                                                                                                                                                                                                                                                                                       |
| Outbound HTTP (Telegram, QStash publish) | **httpx** (async)                                                                                                       | Also the test client.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| QStash signature verify                  | **qstash** (official Upstash Python SDK) `Receiver`                                                                     | Official JWT verification, equals Go `pkg/jobs/receiver.go`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| R2 (S3) presign + delete                 | **boto3** (lazy-imported inside `storage` functions only)                                                               | Proven SigV4; lazy import keeps it off the cold path for non-media routes.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Logging                                  | stdlib `logging` + custom JSON formatter (`countmein/logx.py`)                                                          | One JSON object per line on stdout = what Vercel log drains parse; field names copied from `pkg/logx/logx.go`. No extra dependency.                                                                                                                                                                                                                                                                                                                                                                                         |
| Tests                                    | **pytest 8**, **pytest-asyncio**, **httpx** `ASGITransport`, **fakeredis**, **respx**, real Postgres 17 + Redis 7 in CI | Direct analogues of Go tests (`miniredis` → fakeredis, `httptest` → ASGITransport).                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Lint / types                             | **ruff** (lint + format), **mypy --strict**                                                                             | Required by brief.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Contract diff                            | **oasdiff** (CLI, pinned version via `go`-free binary release)                                                          | Detects breaking changes between the Zod spec and FastAPI's emitted spec.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Local server                             | **uvicorn** on port **3001** (same as `cmd/dev`, `PORT` env override)                                                   | Next dev proxies to the same port; no Next config change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

Python layout (final):

```
apps/web/
  api/
    index.py                 # Vercel entry: `from _lib.countmein.app import app`
    _lib/                    # underscore ⇒ Vercel does not treat contents as functions
      countmein/
        __init__.py
        app.py               # create_app(): routers, middleware, exception handlers, /api/healthz
        config.py            # ← pkg/config
        logx.py              # ← pkg/logx
        contracts/
          models_gen.py      # datamodel-codegen output from openapi.yaml
          constants_gen.py   # ← generate-constants (was constants_gen.go)
          domain.py          # ← pkg/contracts/domain.go (+ flex.go)
          payloads.py        # ← pkg/contracts/payloads.go (x-internal job payloads)
        validation/          # ← pkg/validation (spec.py, decode.py, refine.py, rules.py)
        db/                  # ← pkg/db (one module per Go file; tables.py = SQLAlchemy Table defs)
        redis.py             # ← pkg/redis
        auth/                # ← pkg/auth (loginlink, session, telegram, ticket)
        demo/                # ← pkg/demo
        httpx_/              # ← pkg/httpx (errors, guards, middleware, ratelimit, response); trailing _ avoids clashing with httpx lib
        i18n/                # ← pkg/i18n (translations_gen.py, format, locale, plural)
        jobs/                # ← pkg/jobs
        queue.py             # ← pkg/queue
        storage.py           # ← pkg/storage
        routes/              # ← pkg/routes (auth, bookings, jobs, media, mergepatch, organizers, services, slots)
  tests_py/                  # pytest suite (mirrors package tree) + parity/
  pyproject.toml  uv.lock  requirements.txt  .python-version
  scripts/generate-i18n-py.ts  scripts/generate-constants.ts (retargeted)  scripts/generate-py-models.sh
```

---

## Phase 1 — Discovery & contract mapping

### 1.1 Record baseline

- **Prereq:** clean working tree. **Working directory for this step is the repo root** — `docs/migration` lives at the root, and rule 5's `apps/web` default would create `apps/web/docs/` instead.
- **Actions (repo root):** `mkdir -p docs/migration`; write `git rev-parse HEAD` to `docs/migration/baseline.txt`.
- **Output:** `docs/migration/baseline.txt`.
- **Verify (repo root):** `test -s docs/migration/baseline.txt`.

### 1.2 Endpoint inventory

- **Actions:** Parse `packages/contracts/src/routes.ts` and `apps/web/openapi.yaml`; produce a table of every operation: method, path, auth kind, request schema id, response schema id(s) per status, Go handler (`pkg/routes/*.go` func), guards applied (`pkg/httpx/guards.go`), rate-limit bucket, demo guard, side effects (DB tables written, QStash queue, R2). Include the out-of-spec route `/api/healthz`, marked with an explicit `in_spec=false` column so spec rows and infrastructure rows are counted separately (no "row count minus one" arithmetic). **Do not hardcode an expected operation count** — the script derives it from `openapi.yaml` (at baseline it is 23: auth ×2, organizers ×6, services ×5, slots ×5, bookings ×4, jobs ×1 with queues from `packages/contracts/src/jobs.ts`).
- **Output:** `docs/migration/endpoints.md`.
- **Verify:** `scripts/migration/check-inventory.ts` (run with `bun` from `apps/web`) parses `openapi.yaml` itself and asserts every spec operation appears in `endpoints.md` exactly once, plus the healthz row.

### 1.3 Types, schema, middleware, env inventory

- **Actions:**
  - Map every Go struct in `pkg/contracts`, `pkg/db`, `pkg/jobs`, `pkg/auth` → target Python module/class (`docs/migration/types.md`).
  - Map every DB table/column/index/constraint used by `pkg/db/*.go` against `packages/db` Drizzle schema (`docs/migration/db.md`), incl. every raw SQL string (copy verbatim; they will be reused as `sqlalchemy.text()` or Core expressions).
  - Map middleware chain in `pkg/api/server.go` + `pkg/httpx/middleware.go` in order (`docs/migration/middleware.md`): panic recovery, request id, logging, `_path` rewrite restoration, client IP, rate limit, spec validation, error → JSON mapping. Include the externally visible limits: request body bound at 1MB (`httpx.ReadBodyOr413` → 413), healthz probe timeout 2s, post-commit publish budget 1500ms, media cleanup timeout 3s, outbox mark timeout 5s.
  - Enumerate env vars: `grep -rhoE 'os\.(Getenv|LookupEnv)\("[A-Z_0-9]+"' pkg api cmd | sort -u` **plus** the loop-read names — `config.go`, `api/server.go` and `storage/r2.go` read some variables via `os.Getenv(name)` from slices, which the call-site grep misses; scan those files for `"[A-Z][A-Z0-9_]{2,}"` literals as well. Record required/optional/production-required (`config.Validate`) in `docs/migration/env.md`.
  - Map every Go test file → planned pytest file (`docs/migration/tests.md`), one row per `func Test…`. Note: `t.Run` subtests and table cases are not separate rows — the porting unit is the top-level `func Test`; subtest coverage is preserved by porting the table bodies. To make a lost table case visible, each row carries `cases: N` — the number of `t.Run` subtests / table entries in the Go test (0 for plain tests); the ported pytest must keep at least that many cases.
  - Document Go's JSON wire encoding (`docs/migration/json-encoding.md`): `encoding/json` HTML-escapes `< > &` (`\u003c` …) unless disabled; `time.Time` renders RFC3339Nano; `omitempty` fields vanish, non-`omitempty` pointers render `null`; key order follows struct field order; `json.Encoder` appends a trailing `\n` (note which handlers use `Encode` vs `Marshal` — `httpx.Response.Write` uses `Marshal`, no trailing newline, and sets `Content-Length`). Source: `pkg/httpx/response.go` + golden transcripts.
- **Verify:** `scripts/migration/check-inventory.ts` additionally asserts: every runnable Go test from `go test -list '.*' ./pkg/... ./api/...` (the source of truth — the raw grep overcounts by `TestMain` and build-tagged helpers) appears in `tests.md`, matched on word boundaries (`TestFoo` must not satisfy `TestFooBar`); every env var from both greps appears in `env.md`.

### 1.4 Freeze behavioral fixtures from Go (golden HTTP transcripts)

- **Prereq:** local Postgres + Redis: `docker compose up -d` at the repo root (services `postgres:17`, `redis:7`), then `set -a; source .env; set +a` and `bun run db:migrate` in `packages/db`.
- **Actions:**
  - Write `apps/web/tests_py/parity/scenarios/*.yaml`: ordered request lists covering every endpoint × {happy path, each validation error class, auth missing, demo organizer refusal, 404, 409 delete guard, capacity exhausted, expired manage token, replayed guest ticket, merge-patch absent/null/pair rule, rate limit 429, QStash bad signature, duplicate `outboxId`}. **Time-dependence:** scenarios create their own data with dates relative to `now` and a wide margin (±30 days) — the demo seed is _not_ used as scenario data (it re-lays slots every day from today); "expired token" is forced by a DB update, "past slot" by an explicit old date. Credential minting is a single fixed decision: tickets are written **directly into Redis** with the exact key format of `auth/ticket.go` (`auth:ticket:{token}`, same JSON payload); the organizer session is a JWT minted with the `pkg/authtest` algorithm (HS256, HKDF-derived key) — no Auth.js cookie encoding is needed because the Go API reads the `X-Organizer-Auth` header, not a cookie.
  - Write `scripts/migration/record.py`: starts `go run ./cmd/dev` on port 3101, resets DB (**TRUNCATE of every table in the public schema** except Drizzle's migrations table — a hardcoded list would let a new table stay dirty) **and Redis (`FLUSHDB`) — including rate-limit buckets, tickets and outbox claim keys — before each scenario**, sends requests, stores `{status, headers, body, body_raw}` to `tests_py/parity/golden/<scenario>.json`. **All response headers are recorded except an exclusion list** (`date`, `x-vercel-*`, `content-length`) — the middleware sets security headers the parity check must see. `content-length` is recorded as the **fact** `contentLengthMatchesBody` (header == `len(body_raw)`) plus `transferEncodingChunked: false`, not as a number — Go's RFC3339Nano drops trailing zeros, so timestamp lengths (and thus the header) float run to run; the byte-level pin lives in `body_raw`, which keeps the raw response bytes with the same substitutions, so the byte-level JSON rules of `json-encoding.md` (HTML escaping, key order, trailing newline, spacing) are verifiable in replay. UUIDs, timestamps, tokens, presigned URLs are normalized to placeholders (`<uuid:1>`, `<ts:1>`, stable by first appearance). Timestamp **relations are NOT recorded**: the only timestamp pair any response carries is server-generated `createdAt` next to scenario-authored `startsAt` — a delta that floats run to run and cannot be asserted; and the manage-token **+24h expiry is not on the wire** either (the guest DTO carries only `canCancel`). Timestamp invariants are asserted in the db/contracts tests (`TestCanCancelBooking`), not in HTTP goldens. A step may declare `expectSink: N` — the recorder then waits up to 2s (the publish budget is 1500ms) for exactly N outbound calls and **fails the recording** if fewer arrive, so a late publish can never silently record `sink: []`. **Every step that creates or cancels a booking (POST `/api/bookings`, `/api/bookings/cancel`, `/api/bookings/cancel-by-organizer`) MUST declare `expectSink`** (0 on refusal paths — no publish is itself an assertion); `check-inventory.ts` enforces this. A 429's `Retry-After` is recorded as the class `<retry-after:seconds>` plus the separate checkable fact `retryAfterWithinWindow` (0 < v ≤ the path's rate-limit window).
  - **Outbound stubbing — what is possible via env:** QStash publish is redirected with `QSTASH_URL` to a local recorder (`scripts/migration/sink.py`); R2 presigning is made deterministic with fake credentials (the SigV4 signature is normalized away). **Telegram cannot be redirected** — `telegramAPIBase` is a code constant (`pkg/jobs/telegram.go`), not an env var — so job-handler _send_ behavior is **not** captured in HTTP goldens; it is ported from the Go tests (`pkg/jobs/*_test.go`, incl. `fakeTelegram`) exactly as the rest of the Go test suite. The recorder uses a fake `TELEGRAM_BOT_TOKEN` so widget HMAC is deterministic; job scenarios that would send Telegram messages are covered by the Go tests, not by transcripts.
  - The recorder is Python (run with `uv run --project scripts/migration`) with its **own** `scripts/migration/pyproject.toml` — this is tooling, not the Phase 2 API scaffold, so it does not violate the phase ordering; the API's `pyproject.toml` still arrives in 2.2.
- **Output:** golden transcripts, one per scenario.
- **Verify:** record twice and compare the two golden dirs with `diff -r` (the first copy goes to a fresh `mktemp -d` dir, so reruns never nest directories and parallel runs never collide): `RUN1=$(mktemp -d) && uv run --project scripts/migration scripts/migration/record.py && cp -R tests_py/parity/golden/. "$RUN1"/ && uv run --project scripts/migration scripts/migration/record.py && diff -r "$RUN1" tests_py/parity/golden`. Additionally `check-inventory.ts` verifies golden coverage step by step: the matrix in `endpoints.md` is `operationId × error class → scenario#step → expected status`, and the script asserts the golden transcript at that step really answers that status (file existence alone proves nothing), that every booking create/cancel step declares `expectSink`, and the per-step facts `contentLengthMatchesBody` / `transferEncodingChunked` / `retryAfterWithinWindow`.

### 1.5 Automated contract validation design

- Contract check (implemented in 2.4, enforced in CI): FastAPI emits `app.openapi()`; `scripts/export-py-openapi.py` writes `openapi.py.yaml`; `oasdiff breaking openapi.yaml openapi.py.yaml --fail-on ERR` must pass **and** `oasdiff breaking openapi.py.yaml openapi.yaml --fail-on ERR` must pass (bidirectional = equivalence at breaking level). Additionally a pytest asserts the set of `(method, path)` pairs is identical.
- Shared vectors reused unchanged: `packages/contracts/vectors/{validation,domain}` and `pkg/contracts/testdata/golden` (moved to `tests_py/contracts/golden` in Phase 5).

**Phase 1 gate:** `bun scripts/migration/check-inventory.ts` exits 0; 1.4 determinism check exits 0.

---

## Phase 2 — Project setup & environment

### 2.1 ADR

- Write `docs/decisions/021-api-python-rewrite.md` (Status: Accepted; supersedes ADR-013; amends ADR-016 codegen toolchain and ADR-017 runtime). Content: context, the choice table above, consequences (cold start budget, bundle size, Hobby limits). Mark ADR-013 header `Superseded by ADR-021`.
- **Verify:** `grep -q "Superseded by ADR-021" ../../docs/decisions/013-api-go-rewrite.md`.

### 2.2 Scaffold

- **Actions:** `uv init --lib --no-workspace --python 3.12` is **not** used (it would create `src/`); create files manually:
  - `pyproject.toml`: `[project] name="countmein-api" requires-python=">=3.12,<3.13"`; deps: `fastapi`, `pydantic>=2.7`, `sqlalchemy[asyncio]>=2.0.30`, `psycopg[binary]>=3.2`, `redis>=5`, `httpx`, `qstash>=2`, `boto3`; `[dependency-groups] dev`: `pytest`, `pytest-asyncio`, `fakeredis`, `respx`, `ruff`, `mypy`, `datamodel-code-generator`, `uvicorn[standard]`, `boto3-stubs[s3]`, `pyyaml`, `types-pyyaml`.
  - `[tool.pytest.ini_options] pythonpath=["api"] testpaths=["tests_py"] asyncio_mode="auto"`.
  - `[tool.ruff] target-version="py312" line-length=100 extend-exclude=["**/*_gen.py", "scripts/migration"]` (the Phase 1 recorder is a separate uv project with its own config), `[tool.ruff.lint] select=["E","F","I","B","UP","ASYNC","S","RUF"]`, `[tool.ruff.lint.per-file-ignores] "tests_py/**"=["S101"]` (asserts are the point of tests).
  - `[tool.mypy] strict=true mypy_path="api" packages=["_lib.countmein"] exclude=["_gen\\.py$"]` plus `[[tool.mypy.overrides]]` with an **explicit module list** (`_lib.countmein.contracts.models_gen`, `_lib.countmein.contracts.constants_gen`, `_lib.countmein.i18n.translations_gen`) — mypy rejects `module="*_gen"` patterns (only whole-component `*` is allowed).
  - `.python-version` = `3.12`; `api/_lib/index.py` (the Vercel entry `api/index.py` is created only at the Phase 6 cutover — Vercel auto-discovers `api/*.py` as functions, and a half-ported Python app must not deploy next to the live Go one); `api/_lib/countmein/__init__.py`; `app.py` with `create_app()` returning FastAPI(`openapi_url=None, docs_url=None, redoc_url=None`) — the public spec stays the Zod one.
  - `uv lock && uv export --no-dev --no-hashes --no-emit-project -o requirements.txt`.
  - Append to `apps/web/.gitignore`: `.venv/`, `__pycache__/`, `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`, `openapi.py.yaml`.
- **Verify:** `uv sync && uv run pytest tests_py/test_cold_imports.py` (imports the app in a subprocess; `pythonpath=["api"]` in pytest already covers the sys.path).

### 2.3 Vercel configuration (Hobby tier)

- Edit `apps/web/vercel.json` **in Phase 6 only** (switch-over). In Phase 2, create the target content in `docs/migration/vercel.target.json`:
  - `functions`: `{"api/index.py": {"maxDuration": 10, "includeFiles": "api/_lib/**", "excludeFiles": "{tests_py,node_modules,.next,src,public}/**"}}`. No `memory` key — the Go config's `memory: 1024` is dropped (the key is ignored under fluid compute; omitting it also avoids any Hobby-tier deploy rejection over configurable memory). `maxDuration` 10 matches the current Go budget and stays within Hobby limits.
  - `rewrites`: identical sources to the current file, destination `/api/index?_path=<original>` (same `_path` convention; the ASGI middleware in 3.1 restores `scope["path"]` from `_path` and strips it from the query string, exactly as `api/entry/index.go` does).
- Cold-start rules (enforced by review test `tests_py/test_cold_imports.py`): importing `_lib.countmein.app` must not import `boto3`, `qstash`, or open any network connection; asserted via `sys.modules` **in a fresh subprocess** (in-process `sys.modules` depends on test execution order once Phase 3 adds tests that legitimately import boto3/qstash). Engine/Redis clients are module-level lazy singletons created on first use.
- Target-file sync (enforced by `tests_py/test_vercel_target.py` from Phase 2 on): rewrite sources of `docs/migration/vercel.target.json` must equal the current `vercel.json`'s, every destination must be `/api/index?_path=<source>`, and the function config must keep `maxDuration: 10`, no `memory` key, `includeFiles: api/_lib/**`.
- Bundle-size check: `vercel build` then `du -sm .vercel/output/functions/api/index.func` must be < 200 (MB, headroom under the 250 MB unzipped limit).
- **Verify:** `uv run pytest tests_py/test_cold_imports.py`.

### 2.4 Codegen pipeline (replaces oapi-codegen + Go generators)

- `scripts/generate-py-models.sh`: `uv run datamodel-codegen --input openapi.yaml --input-file-type openapi --output api/_lib/countmein/contracts/models_gen.py --output-model-type pydantic_v2.BaseModel --use-annotated --field-constraints --use-standard-collections --use-union-operator --target-python-version 3.12 --enum-field-as-literal all --use-schema-description --disable-timestamp --strict-nullable --formatters black isort` (formatters pinned: the tool's default is scheduled to change to `builtin`, which would silently reformat the committed output; declare the `datamodel-code-generator[black,isort]` extras).
- `scripts/generate-i18n-py.ts`: copy of `generate-i18n-go.ts` emitting `api/_lib/countmein/i18n/translations_gen.py` (dict literal of ICU messages from `packages/translations`, notifications + `ApiErrors`).
- `scripts/generate-constants.ts`: add Python target `api/_lib/countmein/contracts/constants_gen.py` (keep Go target until Phase 5).
- `package.json` scripts: `"generate:py": "bun run generate:i18n:py && bun run generate:openapi && bun run generate:constants && sh scripts/generate-py-models.sh"`, `"generate:i18n:py": "bun scripts/generate-i18n-py.ts"`, `"test:py": "uv run pytest"`, `"lint:py": "uv run ruff check . && uv run ruff format --check . && uv run mypy"`, `"dev:api:py": "uv run uvicorn --app-dir api _lib.index:app --port ${PORT:-3001} --reload"` (until Phase 6 the entry lives at `api/_lib/index.py`).
- **Verify:** `bun run generate:py && test -s api/_lib/countmein/contracts/models_gen.py && S=$(mktemp -d) && cp -R api/_lib/. "$S"/ && cp openapi.yaml "$S"/ && bun run generate:py && diff -r -x __pycache__ -x openapi.yaml "$S" api/_lib && cmp "$S/openapi.yaml" openapi.yaml` (files created, and re-running produces no diff — compared against a snapshot in a fresh temp dir, like 1.4; a `git status` check would fail before the first commit, which rule 4 forbids the agent from making).

**Phase 2 gate** (all must exit 0):

- 2.1: `grep -q "Superseded by ADR-021" docs/decisions/013-api-go-rewrite.md` (repo root)
- 2.2: `uv lock --check` and `T=$(mktemp) && uv export --no-dev --no-hashes --no-emit-project -o "$T" && diff <(grep -v '^#' "$T") <(grep -v '^#' requirements.txt)` (from `apps/web`; the export is compared ignoring comment lines — the header names the output path, and a `git diff` check is vacuous before the first commit)
- 2.3: `uv run pytest tests_py/test_cold_imports.py tests_py/test_vercel_target.py`
- 2.4: the verify command above
- overall: `bun run generate:py && bun run lint:py && bun run test:py`

---

## Phase 3 — Core implementation

Order is dependency order. For each module: (a) port Go file(s) listed, (b) port its Go tests per `docs/migration/tests.md` into `tests_py/<module>/test_*.py`, keeping test names (`TestFooBar` → `test_foo_bar`), (c) run the verification. Go comments explaining _why_ are ported; do not reference Go files in Python comments.

| Step | Python target                                                                                                | Go source                                                                                   | Specific requirements                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Verify                                                                                                                                                                     |
| ---- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3.1  | `config.py`, `logx.py`, `app.py` skeleton, `httpx_/middleware.py`, `httpx_/response.py`, `httpx_/errors.py`  | `pkg/config`, `pkg/logx`, `pkg/httpx/{middleware,response,errors}.go`, `api/entry/index.go` | `config.validate()` same required-var sets for production / `STRICT_ENV=1`; malformed `APP_URL` rejected. Error envelope JSON byte-identical (key order via Pydantic field order / explicit dict). `_path` restoration middleware. Panic → 500 JSON + log. Exception handlers override FastAPI defaults so no `{"detail": …}` bodies ever appear (includes 404/405 for unknown routes — match Go mux output). JSON responses: `ensure_ascii=False`, `separators=(",", ":")` if Go emitted compact JSON — match golden transcripts. | `uv run pytest tests_py/config tests_py/httpx_`                                                                                                                            |
| 3.2  | `contracts/domain.py`, `contracts/payloads.py`                                                               | `pkg/contracts/{domain,flex,payloads}.go`                                                   | `seats_left`, `slot_end`, `slot_price`, `effective_location/contact`, `can_cancel_booking`, wall-clock ↔ instant using `zoneinfo`. Run shared vectors in `packages/contracts/vectors/domain`.                                                                                                                                                                                                                                                                                                                                      | `uv run pytest tests_py/contracts`                                                                                                                                         |
| 3.3  | `validation/{spec,decode,refine,rules}.py`                                                                   | `pkg/validation/*`                                                                          | Pre-validation transforms (jsTrim semantics = JS `String.prototype.trim` whitespace set, slug lowercase) → Pydantic generated model validation → refinements (reserved slugs from `constants_gen`, options/optionsSelectMode pair). Validation error JSON shape + i18n key identical to Go. All files in `packages/contracts/vectors/validation/*.json` must pass.                                                                                                                                                                 | `uv run pytest tests_py/validation`                                                                                                                                        |
| 3.4  | `db/tables.py`, `db/client.py`                                                                               | `pkg/db/client.go`, Drizzle schema                                                          | `create_async_engine(POSTGRES_URL→"postgresql+psycopg://…", poolclass=NullPool, connect_args={"prepare_threshold": None, "connect_timeout": 5})`. `tables.py` declares Core `Table`s by hand; `tests_py/db/test_tables_match_schema.py` reflects the migrated DB and asserts every declared column's name/type/nullability exists.                                                                                                                                                                                                 | `uv run pytest tests_py/db/test_tables_match_schema.py`                                                                                                                    |
| 3.5  | `db/{shared,organizer,service,timeslot,booking_reads,booking_writes,booking,outbox,media,seed,slice}.py`     | same-name Go files                                                                          | Copy SQL semantics exactly. Atomic reserve as a single `UPDATE time_slots SET booked_count = booked_count + :seats WHERE id = :id AND booked_count + :seats <= capacity RETURNING …` inside the booking transaction — never read-check-write. `hash_manage_token` = SHA-256 hex, parity vector shared with TS. Delete guards count referencing rows and map `psycopg.errors.ForeignKeyViolation` (23503) to the 409 domain error. `photo_url_referenced` prefix check.                                                             | `uv run pytest tests_py/db` (requires `POSTGRES_URL`; tests **fail**, not skip, when `CI=true` and it is missing — same as Go)                                             |
| 3.6  | `redis.py`, `httpx_/ratelimit.py`                                                                            | `pkg/redis`, `pkg/httpx/ratelimit.go`                                                       | Identical key names/TTLs; fail-open on Redis error (ADR-019); `client_ip()` honors `X-Forwarded-For` only on Vercel (`VERCEL=1`) or `TRUST_PROXY_HEADERS=1`.                                                                                                                                                                                                                                                                                                                                                                       | `uv run pytest tests_py/redis tests_py/httpx_/test_ratelimit.py`                                                                                                           |
| 3.7  | `auth/{telegram,ticket,session,loginlink}.py`, `httpx_/guards.py`, `demo/{guard,resolve}.py`                 | `pkg/auth/*`, `pkg/httpx/guards.go`, `pkg/demo/*`                                           | Telegram hash check (HMAC-SHA256, `hmac.compare_digest`), auth_date freshness. Auth.js session decoding must accept the cookie Next.js issues (same algorithm/secret as `session.go`). `require_guest_identity()` consumes ticket single-use (`GETDEL`). Demo id refused on every write + login link. Implemented as FastAPI dependencies.                                                                                                                                                                                         | `uv run pytest tests_py/auth tests_py/demo tests_py/httpx_/test_guards.py`                                                                                                 |
| 3.8  | `i18n/{locale,format,plural,loader}.py`                                                                      | `pkg/i18n/*`                                                                                | Locale resolution cookie `NEXT_LOCALE` → `Accept-Language` → `en`; ICU plural/select subset used by Go ported as-is (no new dependency).                                                                                                                                                                                                                                                                                                                                                                                           | `uv run pytest tests_py/i18n`                                                                                                                                              |
| 3.9  | `storage.py`                                                                                                 | `pkg/storage/*`, `pkg/routes/media.go` cleanup                                              | Same key scheme; presigned PUT params identical (content-type, expiry); `delete_replaced_media` best-effort on a 3s timeout after response (Starlette `BackgroundTask` — runs after body is sent; log failures).                                                                                                                                                                                                                                                                                                                   | `uv run pytest tests_py/storage`                                                                                                                                           |
| 3.10 | `queue.py`                                                                                                   | `pkg/queue/qstash.go`                                                                       | Publish after DB commit, swallow+log errors, `skipped` status when `QSTASH_TOKEN` absent. Outbound via httpx (not SDK) to keep request shape identical; asserted with respx.                                                                                                                                                                                                                                                                                                                                                       | `uv run pytest tests_py/queue`                                                                                                                                             |
| 3.11 | `jobs/*`                                                                                                     | `pkg/jobs/*`                                                                                | Signature verify first (`qstash.Receiver`, current+next keys); `run_job` claims `outboxId` with `SET NX`, releases on retryable failure; Telegram 403/`chat not found` → completed + log; 429/5xx/network → 500. Handlers refetch by id. Response bodies empty.                                                                                                                                                                                                                                                                    | `uv run pytest tests_py/jobs`                                                                                                                                              |
| 3.12 | `routes/{mergepatch,organizers,services,slots,bookings,auth,media,jobs}.py`, `app.py` wiring, `/api/healthz` | `pkg/routes/*`, `pkg/api/server.go`, `adapters.go`                                          | Path/method set equals `openapi.yaml`. Merge-patch: absent = keep, `null` = clear, pair rule both directions, fetch→merge→validate merged→write touched columns only. Healthz: recovers own errors, JSON 503 naming missing vars, IP rate-limited.                                                                                                                                                                                                                                                                                 | `uv run pytest tests_py/routes`                                                                                                                                            |
| 3.13 | contract check                                                                                               | —                                                                                           | `scripts/export-py-openapi.py` builds spec from FastAPI routes **with** `openapi_url` enabled in a test-only factory; run both `oasdiff breaking` directions (1.5); `tests_py/test_route_set.py` asserts identical `(method,path)` set.                                                                                                                                                                                                                                                                                            | `uv run python scripts/export-py-openapi.py && oasdiff breaking openapi.yaml openapi.py.yaml --fail-on ERR && oasdiff breaking openapi.py.yaml openapi.yaml --fail-on ERR` |
| 3.14 | parity replay                                                                                                | —                                                                                           | `tests_py/parity/test_replay.py` replays every scenario against the ASGI app (real Postgres/Redis, outbound sink via respx) and asserts normalized equality with golden files, including recorded outbound calls.                                                                                                                                                                                                                                                                                                                  | `uv run pytest tests_py/parity`                                                                                                                                            |

**Phase 3 gate:** `bun run lint:py && uv run pytest` (all, incl. parity) exit 0; 3.13 command exits 0; every row in `docs/migration/tests.md` has status `ported`.

---

## Phase 4 — Observability, testing & CI/CD

### 4.1 Logging

- `logx.py`: JSON formatter emitting `{"time","level","msg",…extra}` with the same keys as `pkg/logx`; one line per record to stdout; request middleware logs method, path, status, duration_ms, request_id (`x-vercel-id` when present). Secrets never logged: `manageToken`, tickets, `Authorization`, cookies — enforced by `tests_py/test_log_redaction.py`.
- **Verify:** `uv run pytest tests_py/test_logx.py tests_py/test_log_redaction.py`.

### 4.2 Local dev

- Change `apps/web/package.json` `dev` to `concurrently -n web,api -c blue,green "next dev --port 3000" "bun run dev:api:py"`. Playwright E2E (`bun run test:e2e`) must pass against it.
- **Verify:** `bun run test:e2e`.

### 4.3 Invariant test checklist

- `tests_py/test_invariants.py` contains one named test per AGENTS.md convention (e.g. `test_seat_reserve_is_single_conditional_update`, `test_demo_organizer_rejected_on_every_write`, `test_guest_ticket_replay_fails`, `test_notification_failure_never_fails_booking`, `test_duplicate_outbox_delivery_not_resent`, `test_expired_manage_token_refused_on_read_and_cancel`, `test_delete_slot_with_cancelled_booking_is_409`, `test_forwarded_for_ignored_without_trust`, …). Many delegate to module tests; this file is the auditable index.
- **Verify:** `uv run pytest tests_py/test_invariants.py -v`.

### 4.4 GitHub Actions

- In `.github/workflows/ci.yml`, add job `python-api` (keep `go-api` until Phase 5): same `postgres:17` + `redis:7` services and env (`POSTGRES_URL`, `REDIS_URL`, `CI=true`); steps: checkout → `oven-sh/setup-bun@v2` (bun 1.3.14) → `astral-sh/setup-uv@v6` with `python-version: "3.12"`, cache enabled → `bun install --frozen-lockfile` → migrate (`packages/db`: `bun run db:migrate`) → `bun run generate:py` → `git diff --exit-code` (generated artifacts fresh) → `uv lock --check` → `uv export --no-dev --no-hashes --no-emit-project -o requirements.txt && git diff --exit-code requirements.txt` → `uv run ruff check .` → `uv run ruff format --check .` → `uv run mypy` → install oasdiff (pinned release tarball) → contract check (3.13) → `uv run pytest -q`. Trigger: `pull_request` and `push` to default branch (same as existing workflow).
- **Verify:** `actionlint .github/workflows/ci.yml` (run from repo root; install via its release binary if absent) and a green run on the PR.

**Phase 4 gate:** CI `python-api` job green; Go job still green.

---

## Phase 5 — Documentation & deprecation

### 5.0 Parity pre-condition

- Re-run `record.py` against Go at current HEAD and `uv run pytest tests_py/parity`; both must pass. After this step golden files are frozen (Go is no longer available to regenerate them).

### 5.1 Remove Go

- Delete: `apps/web/api/entry/` (`index.go`, `index_test.go`), `apps/web/pkg/`, `apps/web/cmd/`, `apps/web/go.mod`, `apps/web/go.sum`, `apps/web/scripts/build-go.sh`, `apps/web/scripts/generate-i18n-go.ts`, Go target in `scripts/generate-constants.ts`, `.golangci*` if present, Go entries in `.gitignore`, `go-api` job + all `setup-go` steps in `.github/workflows/ci.yml`, Go-related `turbo.json` inputs/outputs, `package.json` scripts `build:go`, `test:go` (`test` becomes `bun run test:web && bun run test:py`).
- Move `pkg/contracts/testdata/golden` → `tests_py/contracts/golden` **before** deleting `pkg/` and repoint tests (`TestGoldenCoverage` → `test_golden_coverage`). Move `pkg/jobs/testdata` similarly.
- Port `pkg/api/vercel_test.go` / `TestHealthzRewrite` to `tests_py/test_vercel_json.py` (every top-level `/api/...` prefix in `openapi.yaml` + `/api/healthz` has a rewrite to `/api/index`).
- **Verify (zero Go artifacts):** from repo root
  `test -z "$(git ls-files '*.go' 'go.mod' 'go.sum' 'go.work' '*.go.tmpl')"` and
  `! git grep -nIiE '\bgo (test|run|build|generate|mod)\b|setup-go|golang|oapi-codegen|kin-openapi|\.go\b' -- ':!docs/decisions/0*' ':!docs/migration'` (historical ADRs 013/016/017 are allowed to mention Go).

### 5.2 Documentation

- Update, stating only deltas: `AGENTS.md` (Stack → API line, Observability, monorepo layout `api/`/`pkg/` block → `api/index.py` + `api/_lib/countmein`, Contracts codegen paragraph: datamodel-codegen replaces oapi-codegen, `bun run generate:py` replaces `build:go`, `*_gen.py` rule, Testing section `test:py`, Conventions references to Go symbols → Python symbols, e.g. `RequireGuestIdentity()` → `require_guest_identity()`, `HashManageToken` in `pkg/db/shared.go` → `hash_manage_token` in `countmein/db/shared.py`), `README.md` (setup: install `uv`, `uv sync`), `docs/architecture.md`, ADR-021 final, any doc under `docs/` referencing `pkg/` paths.
- **Verify:** `! git grep -nE 'pkg/(api|db|httpx|routes|jobs|auth|contracts|validation|queue|storage|i18n|logx|demo|config|redis)/' -- '*.md' ':!docs/decisions/0*' ':!docs/migration'` and `bunx markdown-link-check` on changed `.md` files (or `lychee --offline` if available).

**Phase 5 gate:** `bun run test && bun run lint:py && bun run lint && bun run check-types` exit 0; 5.1 checks exit 0.

---

## Phase 6 — Verification & cutover

### 6.1 Switch Vercel config

- Replace `apps/web/vercel.json` with `docs/migration/vercel.target.json` content. Create the Vercel entry `api/index.py` (re-exporting `from _lib.countmein.app import app`; until now the entry lived at `api/_lib/index.py` so Vercel would not auto-discover a half-ported Python function next to the live Go one), delete `api/_lib/index.py`, and repoint the two places that referenced it: `tests_py/test_cold_imports.py` (`import _lib.index` → `import index`) and the `dev:api:py` script (`_lib.index:app` → `index:app`) — otherwise the cold-import test would check an entry Vercel no longer deploys. `uv run pytest tests_py/test_vercel_json.py`.
- Local build: `bunx vercel build` (requires `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID` in env; `bunx vercel pull --yes --environment=preview` first). Assert `.vercel/output/functions/api/index.func` exists, contains `_lib/countmein/app.py`, and is < 200 MB.

### 6.2 Preview deploy + smoke

- `bunx vercel deploy --yes > .vercel/preview-url.txt` (preview env vars must already exist in the project — required list from `docs/migration/env.md`; check with `bunx vercel env ls preview` and fail listing any missing).
- `scripts/migration/smoke.py <url>` (uses `VERCEL_AUTOMATION_BYPASS_SECRET` as `x-vercel-protection-bypass` header if deployment protection is on): `GET /api/healthz` → 200; `GET /api/organizers/me` unauthenticated → 401 golden body; public read of `/demo` page 200; demo-organizer write → refused body identical to golden; validation-error POST → golden body; POST `/api/jobs/booking.created` without signature → 4xx per golden; measure cold start (first request after deploy) and p50 of 20 warm `healthz` calls — record in `docs/migration/perf.md`; fail if any request exceeds 10 s or cold start > 3 s.
- Parity against preview: replay the read-only and demo-refusal subset of parity scenarios (no destructive writes to shared DB) with `uv run python scripts/migration/replay_remote.py <url>`; must match golden.

### 6.3 Production cutover

- Merge PR only after: CI green, 6.2 green. Production deploy is Vercel's git integration on merge. Immediately run `scripts/migration/smoke.py https://countmein.group`.
- Rollback criterion: any smoke failure, or Vercel logs showing ≥1 5xx rate above 1% within 30 min (`bunx vercel logs https://countmein.group --since 30m` parsed by `scripts/migration/errors_rate.py`). Rollback action: `bunx vercel rollback` to the previous production deployment, then `git revert` the merge commit on a new branch.
- Run QStash schedules sync (`bun run qstash:schedules`) — destination URLs are unchanged (`/api/jobs/{queue}`), so it must report no changes; any change is a failure.

### 6.4 Close-out

- Delete `docs/migration/` scratch artifacts except `perf.md` summary folded into ADR-021 "Consequences"; delete `scripts/migration/` and `tests_py/parity/scenarios` recorder tooling **but keep** `tests_py/parity/golden` + `test_replay.py` as permanent regression tests.
- **Final gate:** `bun run test`, `bun run lint:py`, 5.1 zero-Go checks, production smoke — all exit 0.
