{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "globalEnv": [
    "POSTGRES_URL",
    "REDIS_URL",
    "AUTH_SECRET",
    "APP_URL",
    "R2_ACCOUNT_ID",
    "R2_ACCESS_KEY_ID",
    "R2_SECRET_ACCESS_KEY",
    "R2_BUCKET",
    "R2_PUBLIC_BASE_URL",
    "TELEGRAM_BOT_TOKEN",
    "QSTASH_TOKEN",
    "QSTASH_URL",
    "QSTASH_CURRENT_SIGNING_KEY",
    "QSTASH_NEXT_SIGNING_KEY",
    "NODE_ENV",
    "NEXT_RUNTIME",
    "CI",
    "SENTRY_DSN",
    "SENTRY_ORG",
    "SENTRY_PROJECT",
    "SENTRY_AUTH_TOKEN",
    "NEXT_PUBLIC_POSTHOG_KEY",
    "NEXT_PUBLIC_POSTHOG_HOST",
    "POSTHOG_KEY",
    "GO_API_URL",
    "VERCEL_ENV",
    "NEXT_PUBLIC_SENTRY_DSN",
    "NEXT_PUBLIC_TELEGRAM_BOT_USERNAME",
    "NEXT_PUBLIC_SITE_URL",
    "E2E_BASE_URL"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "!.next/dev/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build", "generate:i18n", "generate:openapi", "generate:constants"],
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/packages/contracts/vectors/**"],
      "outputs": ["coverage/**"]
    },
    "@repo/db#test": {
      "outputs": []
    },
    "@repo/translations#test": {
      "outputs": []
    },
    "test:web": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "test:go": {
      "dependsOn": ["generate:i18n", "generate:openapi", "generate:constants"],
      "inputs": [
        "pkg/**",
        "api/**",
        "go.mod",
        "go.sum",
        "scripts/**",
        "$TURBO_ROOT$/packages/contracts/vectors/**"
      ]
    },
    "generate:openapi": {
      "inputs": [
        "$TURBO_ROOT$/packages/contracts/src/**/*.ts",
        "scripts/generate-openapi.ts",
        "$TURBO_ROOT$/bun.lock"
      ],
      "outputs": ["openapi.yaml", "pkg/api/gen/openapi.yaml"]
    },
    "generate:constants": {
      "inputs": [
        "$TURBO_ROOT$/packages/contracts/src/**/*.ts",
        "scripts/generate-constants.ts",
        "$TURBO_ROOT$/bun.lock"
      ],
      "outputs": ["pkg/contracts/constants_gen.go"]
    },
    "generate:i18n": {
      "inputs": ["$TURBO_ROOT$/packages/translations/**/*.json"],
      "outputs": ["pkg/i18n/translations_gen.go"]
    },
    "test:watch": {
      "cache": false,
      "persistent": true
    }
  }
}
